public enum TileType { Grnd, Wall, Watr }
public enum TileMove { Dev, Land, Watr, Omni, None }

public enum GroundType { Soil, Gras, Watr }
public enum MoveType { Land }
public enum MovePace { Full, Half, Quarter, None }

public enum Targeting { Self, Enemy, Other, AllOther, Field, Area, Random }